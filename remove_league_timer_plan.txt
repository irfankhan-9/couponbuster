# Implementation Plan: Remove League Duration/Week Indicator

## 1. Objective
Remove the visual indicator displaying "Week [X]" or "Off-Season" (accompanied by a calendar icon) from the League Dashboard header. The league logic should rely on Admin closure or capacity limits rather than a displayed timeline.

## 2. Targeted Component
- **File:** `pages/Dashboard.tsx`
- **Component:** `Dashboard`

## 3. UI Modifications
1.  **Locate the Header Statistics Section:**
    - Find the container div with the class `mt-5 flex items-center space-x-8 text-sm` located within the "League Header Card".
2.  **Identify the Element:**
    - Locate the specific child `div` that contains the `Calendar` icon and the conditional rendering `{activeWeek ? Week ... : Off-Season}`.
    - Code snippet to remove:
      ```tsx
      <div className="flex items-center text-slate-500 font-medium">
        <Calendar className="flex-shrink-0 mr-2 h-4 w-4 text-slate-400" />
        {activeWeek ? `Week ${activeWeek.week_number}` : 'Off-Season'}
      </div>
      ```
3.  **Action:** Delete this `div` entirely.
4.  **Cleanup:**
    - Since the "Players Count" badge will be the only remaining item in this row, the `space-x-8` class on the parent container becomes redundant (though harmless). Ideally, remove `space-x-8` to keep the code clean.

## 4. Dependencies Check
- The `Calendar` icon is imported from `lucide-react`.
- **Check:** Search the file for other usages of `Calendar`.
    - It appears to be used in the "Schedule Awaiting Approval" empty state lower down in the file.
    - **Action:** Do NOT remove `Calendar` from the import list if it is still used elsewhere.

## 5. Verification
- **Visual Check:** Navigate to a League Dashboard. Confirm the header only displays the "Players Count" badge under the league title.
- **Layout Check:** Ensure the "Players Count" badge remains left-aligned and isn't displaced awkwardly.